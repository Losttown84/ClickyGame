import React from "react";
import "./jumbotron.css";

const Jumbotron = () => (
  <header className = "header">
    <h1>Metsy Clicky Game</h1>
    <h2>Click a pic to get a point, without clicking the same pic!</h2>
  </header>
);

export default Jumbotron;